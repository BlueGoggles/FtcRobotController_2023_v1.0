package org.firstinspires.ftc.teamcode.samples;import com.qualcomm.robotcore.eventloop.opmode.Autonomous;import com.qualcomm.robotcore.eventloop.opmode.LinearOpMode;import org.firstinspires.ftc.teamcode.FindRegionPipeline;import org.firstinspires.ftc.teamcode.RobotHardware;import org.firstinspires.ftc.teamcode.Utility;@Autonomous(name = "OpenCVFindRegion Linear PoC")public class OpenCVFindRegion_Linear_PoC extends LinearOpMode {	RobotHardware robot = new RobotHardware(this);	@Override	public void runOpMode() {		// Initialize OpenCV		FindRegionPipeline findRegionPipeline = new FindRegionPipeline(Utility.Color.RED);		robot.initializeOpenCV(findRegionPipeline);		sleep(2000);		telemetry.addLine("Ready to run");		telemetry.update();		waitForStart();		while (opModeIsActive()) {			if (findRegionPipeline.getLeftAvgFinal() > findRegionPipeline.getRightAvgFinal()) {				telemetry.addLine("Left Region");			} else {				telemetry.addLine("Right Region");			}			telemetry.addData("leftAvgFinal: ", findRegionPipeline.getLeftAvgFinal());			telemetry.addData("rightAvgFinal: ", findRegionPipeline.getRightAvgFinal());			telemetry.update();			// The OpenCV pipeline automatically processes frames and handles detection		}		// Release resources		robot.releaseResourcesForOpenCV();	}}